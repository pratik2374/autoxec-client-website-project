# Optional: override defaults from project.ts (Studio reads these when running `sanity dev`).
# If unset, studio/project.ts uses your project id fallback — must match website VITE_SANITY_PROJECT_ID.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
